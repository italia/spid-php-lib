1. have the `make post` command wait for successful IdP start (else you may have to repeat the command until the IdP is up and responds with a valid metadata !)
2. to prevent backchannel communication, configure the docker compose so that the nwteork between the two containers is not bridged
